#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main() {
    int n;
    cin >> n; // Read the number of elements in the dataset
    
    // Vector to store the dataset
    vector<int> data(n);
    
    // Read the elements of the dataset
    for (int i = 0; i < n; ++i) {
        cin >> data[i];
    }
    
    // Sort the dataset in non-decreasing order
    sort(data.begin(), data.end());
    
    // Output the sorted dataset
    for (int i = 0; i < n; ++i) {
        cout << data[i] << " ";
    }
    cout << endl;
    
    return 0;
}
